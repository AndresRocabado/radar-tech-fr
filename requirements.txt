# Dépendances d'exécution du dashboard — c'est ce fichier qu'installe
# Streamlit Community Cloud. L'ingestion, la construction de l'entrepôt et les
# tests vivent dans requirements-dev.txt.
#
# Seules les dépendances directes sont listées, à version fixée ; le reste de
# l'arbre est résolu par pip, à l'exception de la pile ML ci-dessous, dont les
# versions conditionnent la compatibilité.

# torch en version CPU : la roue PyPI par défaut embarque CUDA (~2 Go) sous
# Linux, bien au-delà de ce qu'accepte Streamlit Community Cloud.
--extra-index-url https://download.pytorch.org/whl/cpu

# Dashboard
streamlit==1.63.0
duckdb==1.5.5
pandas==3.0.5
numpy==2.5.3
plotly==7.0.0
python-dotenv==1.2.3

# Encodage de la requête des pages « Recherche » et « Assistant ».
# Le modèle (~470 Mo) est téléchargé au premier appel dans le cache Hugging
# Face, jamais versionné.
sentence-transformers==6.0.1
torch==2.14.0+cpu
transformers==5.17.0
tokenizers==0.23.2
huggingface_hub==1.31.0
safetensors==0.8.0
scikit-learn==1.9.1
scipy==1.18.1

# SDK Claude : utilisé seulement si LLM_PROVIDER=anthropic. La démonstration
# publique reste en mode mock ; le paquet est installé d'avance pour qu'ajouter
# une clé dans les Secrets suffise, sans toucher aux dépendances.
anthropic==1.5.0
