# Dépendances de développement : collecte France Travail, construction de
# l'entrepôt et de la taxonomie, tests. Streamlit Community Cloud n'installe
# pas ce fichier — il ne collecte rien, il lit l'entrepôt versionné.
#
#     pip install -r requirements-dev.txt

-r requirements.txt

# Appels à l'API France Travail (src/ingest/).
requests==2.34.2

# Taxonomie de compétences : data/skills_taxonomy.yaml et config/queries.yaml.
PyYAML==6.0.3

# Ligne de commande appelée par n8n (src/cli.py).
typer==0.27.2

# Vérification TLS contre le magasin de certificats du système : nécessaire
# derrière un antivirus ou un proxy qui inspecte le HTTPS, inutile en ligne.
# L'import est optionnel (src/ingest/tls.py) : son absence ne casse rien.
truststore==0.10.4

# Tests.
pytest==9.1.1
